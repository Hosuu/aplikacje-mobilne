export async function GET() {
  // for (let i = 0; i < 10; i++) {
  //   const tag = new Tag({
  //     name: `testTag${i + 1}`,
  //   })
  //   await tag.save()
  // }
  return new Response("Success")
}
